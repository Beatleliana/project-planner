<div class="contenedor-uploads">
    
    <div class="contenedor-aviso">
        <div id="uploads-referentes">
        <h3>Referentes</h3>
        {{#each listaReferentes}}
            <div id="referente">
                <h6>{{this.filename}}</h6>
                <img src="uploads/referentes/{{this}}">
                <button class="boton" onclick="deleteProduct({{this}});">Borrar</button>
            </div>
        {{/each}}
        </div>

    

    <!-- TODO: Implementar con cada carpeta
    <img src="{{{this.imgReferente}}}">
    <img src="uploads/referentes/{{this.filename}}">
    {{#each listaTipografias}}
        <div id="uploads-tipografias">
            <h3>Tipografías</h3>
            <img onclick="showInfo({{this.id}})" src="images/{{this.filename}}">
        </div>
    {{/each}}

    {{#each listaPaletas}}
        <div id="uploads-paletas">
            <h3>Paletas</h3>
            <img onclick="showInfo({{this.id}})" src="images/{{this.filename}}">
        </div>
    {{/each}}

    {{#each listaBocetos}}
        <div id="uploads-bocetos">
            <h3>Bocetos</h3>
            <img onclick="showInfo({{this.id}})" src="images/{{this.filename}}">
        </div>
    {{/each}}
    -->
    
</div>